apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: __SVC__
resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - servicemonitor.yaml
images:
  - name: __SVC__
    newName: ghcr.io/__ORGLC__/svc-__SVC__
    # 첫 빌드의 Bump 가 SHA 로 교체 — 그 전 ErrImagePull 은 정상 (이미지 미존재)
    newTag: bootstrap
